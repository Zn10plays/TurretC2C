from models.MotorStatus import MotorPositionLog
from models.MoveCommand import MoveCommand

# defines precomputed paths for the turret to follow.

class SurvayPath():
    def get_next_setpoint(current_orentation: MotorPositionLog) -> MoveCommand:
        
        pass
